/** Predicted sentiment for document (Negative, Neutral, Positive, or Mixed). */
export enum DocumentSentimentValue {
  Positive = 'positive',
  Neutral = 'neutral',
  Negative = 'negative',
  Mixed = 'mixed'
}
